<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_weatherNearMe"
            android:icon="@drawable/ic_location"
            android:title="@string/weather_near_me" />
    </group>

    <group android:id="@+id/group_preferences">
        <item
            android:id="@+id/nav_theme"
            android:icon="@drawable/ic_theme"
            android:title="@string/dark_mode"
            app:actionLayout="@layout/switch_item"/>
    </group>
    <group android:id="@+id/group_more">
        <item
            android:id="@+id/feedbackItem"
            android:title="@string/help_feedback" />
        <item
            android:id="@+id/aboutItem"
            android:title="@string/about_weather_forecast" />
    </group>
</menu>